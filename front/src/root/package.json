{
  "name": "@my-eduzz/front-root",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "webpack --mode production",
    "start": "webpack serve --open --mode development",
    "pre-commit": "yarn tsc -p ./tsconfig.json --noEmit"
  },
  "dependencies": {
    "@my-eduzz/shared": "*"
  }
}
